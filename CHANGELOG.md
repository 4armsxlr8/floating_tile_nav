## 0.1.0

- Initial release. Publishes `FloatingBottomNavigation`, `FloatingNavigationItem`, and `FloatingNavigationIcon`.
- A single-row floating bottom navigation bar with 2 to 5 items, built-in icons for the default 3 items, configurable colors/dimensions/safe area, and rejection of invalid settings via `ArgumentError`.
